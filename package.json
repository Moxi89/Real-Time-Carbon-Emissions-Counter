{
  "name": "carbon-emissions-counter",
  "version": "1.0.0",
  "description": "Real-time carbon emissions counter",
  "scripts": {
    "build": "mkdir -p dist && cp -r index.html css js _headers _routes.json dist/"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
